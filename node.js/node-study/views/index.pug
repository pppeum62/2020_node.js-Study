html
    head
        title= title
    body
        h1 Hello #{title}!
        ul
            -for(var i=0; i<5; i++)
                li 순서 없는 목록 #{i + 1}
        ol
            -for(var i=0; i<5; i++)
                li 순서 있는 목록 #{i + 1}